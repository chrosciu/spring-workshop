logging:
    level:
        org:
            hibernate:
                SQL: DEBUG
                type:
                    descriptor:
                        sql:
                            BasicBinder: TRACE
spring:
    datasource:
        url: jdbc:tc:postgresql:14.4:///integration-tests-db
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true
