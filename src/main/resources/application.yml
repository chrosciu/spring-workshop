uuid:
  generator:
    enabled: false
incremental:
  generator:
    initial: 100
    step: 5
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE
spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:h2:mem:testdb